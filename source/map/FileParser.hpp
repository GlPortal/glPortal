#ifndef __FILEPARSER_HPP
#define __FILEPARSER_HPP

#include <vector>
#include <cstring>
#include <cstdlib> 
namespace glPortal {
  namespace map {
    class FileParser {
    public:
      FileParser(string filename);
    private:
    }
  }
};

#endif
